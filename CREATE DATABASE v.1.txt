CREATE DATABASE IF NOT EXISTS PaintingDB
;

USE PaintingDB
;

CREATE OR REPLACE TABLE paintings
(	id INT AUTO_INCREMENT, 
	imagefile MEDIUMBLOB NOT NULL,
	year YEAR NOT NULL, 
	artist VARCHAR(80) NOT NULL,
	medium VARCHAR(80) NOT NULL,
	style VARCHAR(35) NOT NULL,
	PRIMARY KEY (`id`))
;
GO

INSERT INTO paintings 
(imageFile, year, artist, medium, style)
VALUES 
('temporaryfilename I used phpmyadmin to add image instead of placing blob here because its effing yuge', 1999, 'TestArtist', 'TestMedium', 'TestStyle')
;
GO